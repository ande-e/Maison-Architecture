export interface Project {
  id: number;
  title: string;
  category: string;
  year: string;
  image: string;
  location: string;
}

export interface Service {
  id: number;
  title: string;
  description: string;
  code: string;
}
